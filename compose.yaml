services:
  birdlist:
    build:
      context: .
      dockerfile: Dockerfile
    platform: linux/arm64
    ports:
      - "${BIRD_LIST_PORT:-8080}:8080"
    environment:
      BIRD_LIST_DB: /data/birds.db
    volumes:
      - ./data:/data
    restart: unless-stopped
